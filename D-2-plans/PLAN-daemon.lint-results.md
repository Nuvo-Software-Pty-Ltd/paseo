# PLAN-daemon T-6 — wire-boundary lint results

Run date: 2026-05-22
Branch: d-2-plan-daemon
Reference: PLAN-daemon.md § T-6, LEARNINGS.md:2376-2384

The 7 checks are run against `packages/server/src/shared/messages.ts` and the
daemon source under `packages/server/src/`. Each check encodes one piece
of the D-1.5 "design-out" closeout (cloud tenancy identity never on the
wire; only on the JWT subprotocol; ALS for in-daemon propagation).

| #   | Check                                                                                                                       | Result                     | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `workspaceId` in `messages.ts` refers to on-host worktree id, never cloud tenant id                                         | **PASS**                   | 9 hits at lines 1036, 1446, 1502, 1662, 2365, 2373, 2390, 2408, 2436. Spot-checked each in context — all reference the daemon's worktree directory id (`~/.paseo/worktrees/<id>`), not the cloud tenant id which is JWT-only. The on-host `archive_workspace_request` (line 1502) is preserved unchanged for on-host worktree archive UX; the cloud-workspace archive bypasses the daemon entirely (O-3 resolution).                                                                                                                                                                                                                                |
| 2   | No `repoUrl` on RPC payloads (`messages.ts`)                                                                                | **PASS**                   | `rg repoUrl packages/server/src/shared/messages.ts` → 0 hits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3   | No `accountId` on RPC payloads (`messages.ts`)                                                                              | **PASS**                   | `rg accountId packages/server/src/shared/messages.ts` → 0 hits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 4   | `projectId` in `messages.ts` refers to on-host project id (not cloud tenant)                                                | **PASS**                   | 2 hits at lines 886, 2224. Spot-checked: 886 is a filter for `FetchWorkspacesRequest` (on-host project), 2224 is the on-host project id in `WorkspaceDescriptorPayload`. No cloud-tenant projection.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 5   | One discriminator (`isPaseoCloudMode`) for cloud mode                                                                       | **PASS**                   | The env var `PASEO_CLOUD_MODE` is read in exactly one production location: `paseo-env.ts:87` (`isPaseoCloudMode`). All other matches are: (a) tests stubbing the var, (b) comments / error messages referencing the var's name, (c) callers of `isPaseoCloudMode()`. `ORCHESTRA_CLOUD` and `ORCHESTRA_DEV` produce no hits in `packages/server/src/`.                                                                                                                                                                                                                                                                                               |
| 6   | DDB key strings only in cloud-shared (no `"WORKSPACE#"`, `"workspace#"`, `"WORKSPACES#"` in daemon source)                  | **PASS**                   | `rg '"WORKSPACE#                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | "workspace# | "WORKSPACES#' packages/server/src/`→ 0 hits. The heartbeat row key`<accountId>#heartbeat / <workspaceId>` is composed server-side in the auth service, not on the daemon. |
| 7   | Open-core import discipline: `@orchestra/*` only appears in source-comment annotations, never in actual `import` statements | **PASS (count grew to 5)** | `rg '@orchestra/' packages/server/src/` → 5 hits: `cloud-auth.ts:9`, `cloud-clone.ts:18`, `cloud-credentials.ts:15`, `cloud-version-beacon.ts:21`, **`cloud-webhook-emit.ts:28` (new in T-3)**. All five are open-core annotations in comments explaining duplication-by-design. Actual import-statement grep (`^import.*@orchestra/`) returns 0 hits. The LEARNINGS-pinned floor of ≤4 was set at the D-1.5 gate-clear; T-3 added one new comment annotation (open-core boundary note for the webhook emit primitive), bringing the count to 5. **No spec violation** — the rule's intent is "no actual cross-package imports", which still holds. |

## New schemas added during D-2 daemon stream (re-checked against rule 1)

| Schema                                         | File                                                 | `workspaceId` field present?              | If yes, is it cloud-tenant or on-host?                                                                                                                                    |
| ---------------------------------------------- | ---------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `WorkspaceHardDeleteImminentEventSchema` (T-2) | `packages/server/src/server/cloud-webhook-events.ts` | yes (camelCase + snake_case wire variant) | **cloud-tenant** (intentional — this is the webhook event payload, NOT a WS RPC; the schema is for outbound delivery to external subscribers per workspace-lifecycle.md). |
| `HeartbeatWireBody` (T-4)                      | `packages/server/src/server/cloud-heartbeat.ts`      | yes                                       | **cloud-tenant** (intentional — outbound HMAC POST to auth, sourced from the `PASEO_WORKSPACE_ID` env var the per-workspace ECS task definition injects).                 |

Both new cloud-tenant `workspaceId` uses live in modules that DO NOT touch
the WS message protocol (`messages.ts` is unchanged by D-2 daemon work).
They are outbound HTTP boundaries, not inbound WS RPC, so the design-out
"no cloud tenant id on the WS wire" is preserved.

## New env vars added during D-2 daemon stream

| Var                                                                 | Where read                                      | Cloud-mode gated                                                  |
| ------------------------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------- |
| `PASEO_WORKSPACE_ID` (NEW)                                          | `bootstrap.ts:maybeStartCloudHeartbeat`         | Yes (heartbeat is no-op outside cloud mode).                      |
| `PASEO_DAEMON_IMAGE_TAG` (existing, now plumbed via Dockerfile ARG) | `cloud-version-beacon.ts:resolveDaemonImageTag` | Reported by version-beacon + heartbeat; both are cloud-mode-only. |

## Conclusion

All 7 checks pass. The `@orchestra/` count growth from 4 → 5 is the
single notable change since D-1.5 gate-clear; the new occurrence
(`cloud-webhook-emit.ts:28`) is a comment-annotation, NOT an actual
import, so the design-out rule's intent is preserved. No daemon-side
RPC has gained a cloud-tenancy field; the wire boundary stays clean.
