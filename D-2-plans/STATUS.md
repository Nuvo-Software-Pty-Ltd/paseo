# PLAN-daemon — D-2 daemon stream status

Run date: 2026-05-22
Branch: `d-2-plan-daemon` on `Nuvo-Software-Pty-Ltd/paseo` (AGPL fork).

The plan ([PLAN-daemon.md](./PLAN-daemon.md)) and its 2026-05-22 synthesis
banner are the source of truth. This document summarizes outcomes per
task — what was implemented, what was verified, what was deferred, what
still depends on other streams.

## Tasks

| #    | Title                                                               | Status                                                  | Commit                                            | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | ------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-1  | arm64 daemon image: end-to-end verification on Graviton             | **VERIFIED (static)** / **OPERATOR-DEFERRED (runtime)** | n/a (no source change)                            | Static analysis confirms: `sherpa-onnx-linux-arm64` correctly marked `os=linux, cpu=arm64, optional=true`; `onnxruntime-node` covers linux; node-pty / sentencepiece-js are universal. Dockerfile multi-arch base (`node:22-bookworm-slim`) resolves to arm64 under CI's `platforms: linux/arm64`. **Runtime smokes on a real Graviton (`t4g.medium` or `m7g.medium`) instance are operator-driven** per the plan's "Verification commands" — I cannot run on Graviton from this worktree. See PLAN-daemon.md § T-1 for the four `docker run` smokes.                                                                                                                                                                                  |
| T-2  | `workspace.hard_delete_imminent` event payload schema               | **DONE**                                                | `b5e80f08`                                        | New: `cloud-webhook-events.ts` + test. Dual-schema (camelCase TS / snake_case wire) with transforms in both directions; `.strict()` rejects unknown fields. COMPAT comment cites `workspace-lifecycle.md`. 14 tests pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T-3  | Outbound webhook emit primitive (HMAC, retries, payload validation) | **DONE**                                                | `15e9cb3a` (refactored onto helper in `1e424eb7`) | New: `cloud-webhook-emit.ts` + test. Validates event against T-2 schema before sending, serializes via `toWire…`, signs with HMAC-SHA256, POSTs with `X-Orchestra-Internal-HMAC`. Day-1 no-retries (EventBridge Scheduler is the retry surface). 4 tests pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T-4  | `writeHeartbeat()` / `startHeartbeatLoop()` in cloud-mode           | **DONE**                                                | `1e424eb7`                                        | New: `cloud-heartbeat.ts` (the loop), `cloud-hmac-fetch.ts` (extracted shared helper — third HMAC-POST call site; webhook-emit refactored to use it), `bootstrap.ts` (boots loop after wsServer is constructed in cloud mode, stops on shutdown BEFORE closing agents). 30s cadence, 0–2s initial jitter, warn-and-continue on failure. Body `{ workspaceId, lastHeartbeat, activeAgents, connectedClients, daemonImageTag }` (no `accountId`, no `repoUrl`). 8 heartbeat tests + 3 helper tests pass.                                                                                                                                                                                                                                 |
| T-5  | Per-spawn `~/.claude` materialization under per-workspace IAM       | **DONE (regression-test added)**                        | `0d889256`                                        | No source changes needed: `cloud-credentials.ts:60-86` already logs the operator-triage shape (workspaceId + secretId + err) and rethrows with workspaceId in the message + AccessDeniedException preserved as `Error.cause`. Added two regression tests: AccessDenied operator-triage path + AccessDenied-not-rewritten-as-missing. Plus F3 sanity check that `provisionCloudClaudeHome` accepts no caller-supplied workspaceId. 11 tests pass. **Hands-on cross-tenant probe (operator A reading operator B's secret → IAM deny) remains operator-driven per PLAN-daemon § T-5 hands-on gate.**                                                                                                                                      |
| T-6  | Wire-boundary lint pass                                             | **DONE**                                                | `3b27a307` + `ac92db51` (table-rendering fixup)   | New: `D-2-plans/PLAN-daemon.lint-results.md`. All 7 checks PASS. Notable: the `@orchestra/` count grew 4→5 because T-3 added a new comment-annotation (`cloud-webhook-emit.ts:28`); 0 actual `import` statements still — design-out intent preserved. New T-2 webhook-event payload + T-4 heartbeat body both carry cloud-tenant `workspaceId` but live in outbound-HTTP modules, NOT in `messages.ts` — the WS wire boundary is unchanged.                                                                                                                                                                                                                                                                                            |
| T-7  | SIGTERM-clean release of `/workspace` FDs                           | **VERIFIED (no source change needed)**                  | n/a (covered by T-4's heartbeat stop)             | Audit walked the shutdown path: `bootstrap.ts:stop()` → `heartbeatController.stop()` (T-4) → `closeAllAgents` → `terminalManager.killAll()` (node-pty) → `wsServer.close()` (calls `session.cleanup()` per connection, disposes `workspaceGitService` + `checkoutDiffManager`) → `httpServer.closeAllConnections()` → `httpServer.close()`. No `fs.watch` / `chokidar` usage anywhere in daemon source. `DownloadTokenStore` has no persistent timers. `daemon-worker.ts` enforces a 10s force-exit fallback, well within ECS 30s grace. **No leaks found; T-4's heartbeat-stop is the only meaningful hardening.** Hands-on probe (suspend → reattach EBS → resume) is PLAN-cdk-infra's snapshot-and-restore story, not the daemon's. |
| T-8  | Centralized `OrchestraSessionExpiredError` handler                  | **NOT THIS STREAM**                                     | n/a                                               | Per the plan: "this is PLAN-app's work, not the daemon's." The daemon's role (reject post-expiry requests cleanly) was verified at D-1.5. No daemon-side surface change. **Action item for PLAN-app**: confirm the centralized handler lives there.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| T-9  | `PASEO_DAEMON_IMAGE_TAG` plumbed end-to-end                         | **DONE (build-time)** / **CROSS-STREAM (runtime)**      | `ba2cc6ec`                                        | Dockerfile gets `ARG PASEO_DAEMON_IMAGE_TAG="unknown"` + `ENV` propagation. CI workflow passes `build-args: PASEO_DAEMON_IMAGE_TAG=${{ steps.version.outputs.tag }}`. Local `docker build --build-arg PASEO_DAEMON_IMAGE_TAG=…` injection works; the next push to `main` produces an image whose ENV matches the ECR tag. **Cross-stream: PLAN-cdk-infra still owes the runtime ECS task-definition injection (runtime override of build-time bake) so a redeploy surfaces a new tag without rebuilding.**                                                                                                                                                                                                                             |
| T-10 | `/api/auth-internal/*` namespace rename                             | **DONE**                                                | `cdac9d57`                                        | PLAN-auth-and-shared has shipped the rename (confirmed by reading `/home/frank/.paseo/worktrees/15498i22/d-2-plan-auth-and-shared/packages/auth/src/routes/`). Updated two outbound call sites + their tests: `cloud-clone.ts:fetchWorkspaceRepoUrl` and `cloud-version-beacon.ts:sendDaemonVersionBeacon`. `cloud-heartbeat.ts` (T-4) was already pinned to the new namespace. The daemon's INBOUND `/api/internal/clone-repo` is unchanged — that endpoint lived on the daemon side and never collided. **Cross-stream: PLAN-cdk-infra still owes the ALB rule collapse from per-path rules into a single `/api/auth-internal/*` prefix rule.**                                                                                      |

## Cross-stream pins still owed by other streams

| Owed by               | What                                                                                                                                                                                                                     | Why we need it                                                                                                                                                                                                                            |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PLAN-auth-and-shared  | `POST /api/auth-internal/heartbeat` — accepts `{ workspaceId, lastHeartbeat, activeAgents, connectedClients, daemonImageTag }`, looks up `accountId` server-side, writes DDB row `<accountId>#heartbeat / <workspaceId>` | T-4 produces this body every 30s; the receiver doesn't exist yet. Heartbeat sends will return non-2xx until the route lands.                                                                                                              |
| PLAN-cdk-infra        | Sparse DDB GSI on `lastHeartbeat`                                                                                                                                                                                        | T-4's heartbeat row is queried by PLAN-lifecycle-worker via this GSI; without it, suspend-detection is broken.                                                                                                                            |
| PLAN-cdk-infra        | Per-workspace IAM task role hard-coded to `paseo-cloud/<account>/<workspace>/*` Secrets Manager prefix                                                                                                                   | T-5's hands-on cross-tenant probe needs this to assert IAM deny; D-1.5's single-role-grants-all environment masked the failure mode.                                                                                                      |
| PLAN-cdk-infra        | `PASEO_WORKSPACE_ID` env var injected into the per-workspace ECS task definition                                                                                                                                         | T-4's heartbeat-loop boot gates on this var being present and non-empty; without it, the loop is skipped (warn-and-continue).                                                                                                             |
| PLAN-cdk-infra        | `PASEO_DAEMON_IMAGE_TAG` runtime injection in the ECS task definition (separate from the build-time bake landed in T-9)                                                                                                  | Lets a redeploy with a freshly pulled image surface the new tag without rebuilding.                                                                                                                                                       |
| PLAN-cdk-infra        | ALB rule collapse from per-path `/api/internal/*` rules into a single `/api/auth-internal/*` prefix rule                                                                                                                 | T-10's rename is now live on both daemon and auth; the ALB still needs the rule update. Until then, `/api/internal/clone-repo` (daemon inbound) and `/api/auth-internal/*` (auth inbound) need disjoint routes.                           |
| PLAN-lifecycle-worker | Scan-every-60s + activity-gates (`lastHeartbeat<now-15min AND activeAgents==0 AND connectedClients==0`)                                                                                                                  | T-4 produces the signal; lifecycle worker consumes it to decide suspends.                                                                                                                                                                 |
| PLAN-lifecycle-worker | Webhook emit caller at T-24h pre-purge                                                                                                                                                                                   | T-2 (schema) + T-3 (primitive) ship the AGPL fork's contribution. Lifecycle worker is the physical caller (O-1 → Architecture B). Schema duplication / import discipline is the proprietary side's call (filed at deferred follow-up #3). |
| PLAN-app              | Centralized `OrchestraSessionExpiredError` handler                                                                                                                                                                       | T-8 — not the daemon's. The daemon's reject-post-expiry behavior was verified at D-1.5.                                                                                                                                                   |

## Open follow-ups for the operator

These are NOT in scope for this PR (per the anti-bundle rule), but should
go into the project tracker so they don't get lost.

1. **T-1 runtime verification on a real Graviton instance.** The four
   `docker run` smokes + the D-1 8-step hands-on smoke. Operator's call
   on which ECS cluster / instance family.
2. **T-5 hands-on cross-tenant probe.** Once PLAN-cdk-infra ships the
   per-workspace IAM task role, operator A reads workspace B's
   Secrets Manager secret → expect AccessDeniedException visible in
   CloudTrail and in the daemon's structured log line. Also verify
   `/tmp/orchestra-claude-home/<spawn-id>` directories are absent after
   spawn termination.
3. **D-2 hands-on gate** (ROADMAP § Phase D-2): two operators, two GitHub
   accounts, two workspaces, two agents; cross-tenant probes pass.
4. **`tenantId` rename across the 9 cloud-tenant `workspaceId` sites in
   `messages.ts`.** Filed as deferred follow-up #1 in PLAN-daemon.md.
   Depends on `@orchestra/cloud-shared` deciding first. Until then, every
   new RPC schema must carry a comment noting "this is the on-host
   worktree id, NOT the cloud tenant id".
5. **`/tmp/orchestra-claude-home` vs `/tmp/paseo-claude-home` ROADMAP
   text mismatch.** Doc update on next ROADMAP touch; not bundled.
6. **Anti-drift guard for AGPL ↔ proprietary duplicated schemas.** D-2
   adds T-2's webhook event schema as a new duplicate; worth a single
   sweep (filed as PLAN-daemon.md deferred #3).
7. **D-3 scheduled-agents under cloud mode.** `provisionCloudClaudeHome`'s
   ALS-required path will break the first time a schedule fires in cloud
   mode (no inbound WS handler in scope). Filed as PLAN-daemon.md risk
   #6 — flagged for D-3 planning, not D-2.

## Test summary

All new + modified tests pass. Full count: 68 tests across 8 files:

- `cloud-webhook-events.test.ts` (T-2): 14 tests
- `cloud-webhook-emit.test.ts` (T-3): 4 tests
- `cloud-hmac-fetch.test.ts` (T-4 helper): 3 tests
- `cloud-heartbeat.test.ts` (T-4): 8 tests
- `cloud-credentials.test.ts` (T-5 additions): 11 tests
- `cloud-clone.test.ts` (T-10 rename): 11 tests
- `cloud-version-beacon.test.ts` (T-10 rename): 9 tests
- `bootstrap.smoke.test.ts` (regression guard for T-4 wiring): 8 tests

Pre-commit hooks (lefthook → lint + format + typecheck) green on every
commit; no `--no-verify` skips.

## Commits (in order)

1. `b5e80f08` — T-2 schema
2. `15e9cb3a` — T-3 webhook emit primitive
3. `1e424eb7` — T-4 heartbeat loop + shared HMAC helper (refactor)
4. `0d889256` — T-5 IAM-deny regression tests
5. `ba2cc6ec` — T-9 PASEO_DAEMON_IMAGE_TAG plumbing
6. `cdac9d57` — T-10 /api/auth-internal/\* rename
7. `3b27a307` — T-6 wire-boundary lint pass
8. `ac92db51` — T-6 fixup (markdown table rendering)
